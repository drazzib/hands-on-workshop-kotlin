info:
  name: Get pet name
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8081/api/v1/pet-name?kind=cat
  params:
    - name: kind
      value: cat
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
