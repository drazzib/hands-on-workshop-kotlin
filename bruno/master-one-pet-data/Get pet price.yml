info:
  name: Get pet price
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8081/api/v1/pet-price?kind=cat
  params:
    - name: kind
      value: cat
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
