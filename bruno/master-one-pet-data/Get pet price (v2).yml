info:
  name: Get pet price (v2)
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8081/api/v2/pet-price?kind=dog
  params:
    - name: kind
      value: dog
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
