info:
  name: Create Dog (no name)
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8080/api/pets
  body:
    type: json
    data: |-
      {
        "kind": "DOG"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
